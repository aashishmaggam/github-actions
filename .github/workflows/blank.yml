name: output
on: 
  push:
    branches:
      - main
      - test
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: sth
        if: github.ref=='ref/heads'
        run: echo "sucess"
      - name: ft
        run: echo "not sucess"
    
