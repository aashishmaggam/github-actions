name: git
on: 
 workflow_dispatch:
   inputs:
     environment:
      description: "Select environment"
      required: true
      default: dev
jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - name: deploy the application
        run: echo " deploying app into ${{ inputs.environment}}"
